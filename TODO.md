# Graphiq Studio — Roadmap / TODO

Everything that would take the app from "feature-complete editor" (see [FEATURES.md](FEATURES.md)) to a great, professional-grade photo tool. Grouped by area; roughly ordered **P1 → P3** inside each group (P1 = finish/polish what exists, P2 = expected pro features, P3 = platform leaps).

Legend: `[ ]` open · `[~]` partially exists (noted) · `[x]` completed.

---

## 1. Known gaps & unfinished edges (P1 — finish what's started)

- [x] **Linked-mask move** — done: a whole-layer Move now shifts a linked mask with the pixels, folded into the same one-step undo.
- [x] **Blur / Dodge-Burn on masks** — done: both brushes target the mask when it's the active surface (mask-surface history entries, live alpha rederive).
- [x] **Smart-filter stack mask** — done 2026-07 (this line was a stale duplicate; see the completed entry in §Roadmap below, item 2). Every part of the 2026-07 assessment shipped: engine mask surface under `fm:<id>` reusing the whole layer-mask machinery, `ActiveSurface` gained `"filterMask"`, history's mask surface covers it, `.gproj` v8 serializes `filterMaskImage`, and both the Layers-panel flask chip (click = paint, Shift-click = enable/disable) and the Smart Filters dialog (Add / From Selection / Delete) manage it. Verified in code 2026-07-12. Still open (tracked separately): unlinked filter-mask move.
- [x] **Per-filter drag-reorder** — done 2026-07-18: the Smart Filters dialog's stack rows are now **draggable** (HTML5 DnD, same visual language as the Layers panel — the dragged row dims via `data-dragging`). The order **previews in local state while dragging** (midpoint-based before/after in DISPLAY space, which is the reversed array — "above the target" means later in the render order) and commits as **ONE "Reorder Smart Filters" history step on drop**; dropping nowhere or pressing Esc cancels via `dragend`'s `dropEffect === "none"` and discards the preview. Dragging also selects the row; the Up/Down buttons remain for keyboard-friendly nudges. No engine work — reorder still flows through the existing `onCommit` path.
- [x] **On-canvas anchor drag for smart blurs** — done 2026-07-20. The Smart Filters dialog's Centre group (zoom/spin/tilt-shift) gained a **"Set on canvas"** crosshair toggle with the Curves-TAT treatment: while armed the dialog **docks right at 96% opacity** with the blanket click-through (overlay `pointerEvents:none`, dialog re-enables them), the canvas cursor turns crosshair, and **click-dragging the image places the anchor live** — CanvasArea only captures the pointer and reports doc-normalized (0–1, clamped) coords; Editor patches the armed filter through the SAME debounced `setFiltersLive` path as the X/Y sliders, so it's one history step per gesture and the sliders track the drag live. **The Blur Gallery's guides now draw on the document itself** (marching-ants overlay, kept alive while armed): centre reticle for zoom/spin; for tilt-shift the focus centre line, solid band edges and dashed feather edges — the exact filters.ts geometry (offsets are % of min(docW,docH)/2, line along `(cos θ, sin θ)`) — dual-stroked dark-under-white and clipped to the artwork. Tool hover rings (brush/heal/red-eye/blur/dodge/clone) are suppressed while armed (a stale brush ring next to the reticle read as a second guide — caught in-browser). **Esc exits targeting first**, closing the dialog only on a second press; picking another filter row, switching kind to a non-radial, removing the filter, undo, or closing the dialog all disarm — the guide re-derives from the layer tree every render so it can never go stale. Non-left buttons fall through (middle-drag pan keeps working while armed). Verified against the running app (headless Edge): a canvas drag landed the sliders on exactly X 60% / Y 70%, tilt-shift band lines match the filter's math at Focus 20/Feather 30, Esc layering works, zero console errors.
- [x] **Curves targeted adjustment** — done 2026-07: a **Target** toggle (crosshair) in the Curves dialog's toolbar arms Photoshop-style TAT — **click a tone on the image, drag up/down to brighten/darken the curve at exactly that tone** (1 px ≈ 1 output unit; up = brighter). While armed the dialog **docks to the right edge** with the blanket click-through (overlay `pointerEvents:none` + the dialog re-enabling them — unlike the Levels droppers' one-shot full click-through, TAT is a persistent mode so the chips/Reset/Done must stay clickable), the canvas cursor turns crosshair, and **Esc exits targeting first**, closing the dialog only on a second press. **The point lands at the PRE-curve tone**: the sampled view pixel is `channel(master(raw))`, so the effective per-channel LUTs are **inverted** (nearest-value scan over 256 entries — robust for rising, falling AND plateaued curves) to recover `raw`; the master tab uses `luma(raw)` (0.299/0.587/0.114), a colour tab uses `master(raw)` — the domain that channel's curve actually sees (tone.ts composes master-first). Clicking within 6 units of an existing point grabs it; otherwise a new point is inserted **on the current curve** (`curveSampler(pts)(x)`) so nothing jumps until the drag moves. Drags flow through the existing `onToneChange` (node mode: debounced one-step history; dest mode: live engine preview) — CanvasArea just captures the pointer and reports start-RGB + Δy (same plumbing family as the Levels eyedroppers, per the assessment). 7 numeric checks (scratchpad verify-curvetarget.ts) pin the inversion round-trips (identity exact, S-curve ≤ 1, negative + plateau sane), the master(raw) domain mapping, and the on-curve insert (curve moves ≤ 2 units). Honest limits: samples the flattened VIEW, so layers/effects ABOVE a curves node (or an adjustment mask at that pixel) can skew the recovered tone; Levels has no TAT (its params aren't per-tone).
- [x] **Gradient stroke layer-effect UI** — done 2026-07-19. The Layer Style dialog's Stroke section gained a **Fill: Colour / Gradient** segmented; gradient mode swaps the colour well for the **full `GradientEditor`** (the Gradient tool's multi-stop editor with the shared preset library via `gradientStorageKey`), plus **Reverse** and an **Angle** slider. Switching to gradient **seeds from the current stroke colour** (colour → black) so the stroke never blanks; switching back restores colour mode with the colour intact. Engine ([effects.ts](app/lib/effects.ts)): `StrokeFX` gained optional `angle`/`reverse`; the renderer now uses the **gradient overlay's centred linear geometry** when `angle` is present — and keeps the legacy top-left→bottom-right diagonal when it's ABSENT, so any document saved before the angle existed renders byte-identically (the field only appears once the UI writes it). Effect-cache keys hash the effects object, so the new fields invalidate automatically.
- [x] **Clipping edge case (§16.9)** — done 2026-07-19, with Photoshop-faithful semantics. A clipped layer stranded directly above a **non-clipped adjustment** now **walks down past the whole adjustment run** to the first non-clipped pixel node (layer OR group) and borrows its silhouette — the previously-inert clip works. Model ([layers.ts](app/lib/layers.ts), 10-check verify-clipgroups.ts): `ClipGroup` gained `maskFrom?`; `clipGroupsOf` sets it on the stranded run's unit (the run's contiguous clipped members stay with it). Rendering ([paint.ts](app/lib/paint.ts) `drawBorrowedClipRun`): the run renders **at its OWN stack position** — crucial — so the adjustments beneath still apply to the base but NEVER to the run's pixels (exactly Photoshop), each node compositing in place with its own blend/opacity (so a Multiply member multiplies against the already-adjusted backdrop), masked per-node by the borrowed base's intrinsic silhouette (`renderNode(maskFrom)` — styled ⊗ masked, groups lend theirs too); a hidden borrowed base hides the whole run. **Clipped adjustments inside the run** work too: `applyAdjustmentNode` gained an optional `clipAlpha` folded into its modulation alpha (fast path correctly skipped when set). Honest bounds (all tested): a clipped node at the bottom of the list, or with ONLY adjustments below, stays inert as before; a **clipped** pixel node below the adjustment does NOT lend its silhouette (it belongs to another group — that doubly-nested case stays inert, documented); rendering is deliberately uncached (the node intrinsics behind it still cache; cache-key safety holds because `clipped` participates in every node's effective key and the borrowed base is a lower sibling in every belowSig).
- [x] **Channels panel: mask channel + Alt-click mask view** — done 2026-07-18. **Engine mask-view mode** (paint.ts): `setMaskView(id, "mask"|"filterMask")` short-circuits `composite()` — the view canvas shows the mask's **effective grayscale** (R×A/255, the exact math `deriveMaskAlpha` uses, so erased-transparent regions read black consistently) instead of the composite; a **live stroke targeting that mask previews through the same `drawStroke` path** the normal view uses, so painting while viewing works; the branch clears `pendingDirty`/`lastTree` so toggling off does a clean full blit, and CanvasArea's engine-wiring effect calls `setMaskView(null)` on every doc (re)activation — the flag is per-engine and must never survive a tab switch stale. New `maskHistogram(id)` verb (256-bin tonal counts of the effective grayscale). **Alt-click the mask chip** in the Layers panel toggles the view (`LayersApi.maskViewId/toggleMaskView`; viewing also targets the mask for painting, Photoshop-style); the chip shows a **dashed accent ring + glow** while viewing, and Editor auto-exits when the layer/mask disappears, on doc switch, or when `chooseSurface` picks anything that isn't the viewed mask ("click a thumbnail to come back"). **Channels panel**: when the active layer has a mask it appears as a fourth channel row — a "Mask" curve toggle (neutral-gray dot, drawn source-over beneath the additive RGB curves with its OWN normalization so a mostly-white mask doesn't flatline the graph) and an **eye button** driving the same on-canvas view; recomputes ride the panel's existing debounced engine subscription plus the active-layer/mask-presence dep. Honest limits: layer masks only in the UI (the engine verb accepts filter masks for a future chip), no rubylith overlay mode, and the mask view is session-only by design.
- [x] **Options-bar "editing mask" pill** — done 2026-07-20. An accent-bordered pill in the options bar (pinned between the tool badge and the scrolling controls, so it can't scroll away) reads **"Editing mask"** (mask-chip glyph) or **"Editing filter mask"** (flask glyph) whenever the active layer's paint target is a mask. It renders from the SAME `paintSurface` state that drives the Layers-panel target ring, so the two indicators can never disagree; it shows for every tool, deliberately — fills and strokes all route through the engine's `surfaceTarget` chokepoint, so the mode matters beyond brushes. **Clicking the pill returns to pixel editing** (small ✕ affordance; goes through `chooseSurface`, so it also exits mask VIEW mode exactly like clicking a pixel thumbnail). Verified in-browser: no pill on a maskless layer, appears on Add Layer Mask, dismisses back to pixels on click, no console errors.
- [x] **Liquify** — done 2026-07-20. **Effects ▸ Liquify…** (`Ctrl+Shift+X` now actually wired via the menu-action registry, so it's in the `Ctrl+K` palette and the Shortcuts window too) opens a modal warp-mesh editor on the **active pixel layer** (adjustments/groups toast "select a pixel layer"; vector text/shape layers toast "rasterize first" instead of silently desyncing their recipe). **Math** ([liquify.ts](app/lib/liquify.ts), pure + Node-tested): a regular displacement grid in doc space (spacing ≈ max-dim/240 ⇒ 8px on 1920×1080) holding a **backward** field — D(p) is "sample from p + D(p)" — so rendering is a hole-free **premultiplied-bilinear** backward warp with a transparent border (edge pulls bring clean transparency, soft alpha never halos). All five brushes from the spec, cosine-falloff, accumulate on the field: **forward warp** (per-move deltas, capped at the brush radius so fast strokes can't tear), **pucker/bloat** (radial, hold-to-build at 40ms ticks), **twirl** (per-node partial rotation; **Alt reverses**), **reconstruct** (fades D toward identity). **Dialog** ([LiquifyDialog.tsx](app/components/LiquifyDialog.tsx), Blur-Gallery styling): 560×430 letterboxed live preview (the doc-space mesh renders a downscaled source through a scale factor — verified consistent with full-res), tool grid, **Size/Pressure** sliders with **pen pressure** multiplying strength, brush ring, and a **Show mesh** overlay drawing the grid's first-order forward image (p − D(p), so a bloat visibly bulges outward rather than showing the inverse pinch), dual-stroked to read on any content; Enter applies / Esc cancels (captured). **Mesh save/load**: `.gmesh` JSON (magic + version + base64 Float32 planes — bit-exact round-trip) with clear rejection messages for malformed files; loading **resamples onto the current doc size** (normalized-coord bilinear, axis-scaled vectors) so a saved warp applies proportionally anywhere; Reset returns to identity. **Apply** renders full-res once and lands as **ONE "Liquify" undo step** via `applyLayerImage`; new engine verb `getLayerCanvas` (doc-sized copy, blank for still-empty layers). **Verified**: 25 numeric checks (identity byte-exact; bloat/pucker area growth/shrink; warp direction + vacated trail; twirl asymmetry + direction flip; falloff isolation byte-exact outside the radius; reconstruct → <5% residual; edge transparency; preview-scale ↔ full-res agreement; serialize round-trip; five garbage-rejection cases; resample vector scaling; mesh-line geometry) plus an in-browser run (guard toast, bloat + warp on a painted stroke, mesh overlay, Apply baked onto the real canvas, zero console errors) — which caught a real StrictMode bug (rAF handle not reset in the effect cleanup → the preview never painted in dev). **Honest limits**: strokes compose additively on one field (not true per-stroke forward composition — extreme stacked warps can drift from PS behaviour), no freeze mask / selection pre-freeze, pressure scales strength only (not size), the mesh is session-only unless saved to a file (not serialized in `.gproj`), and Apply is destructive — a non-destructive smart-liquify node would ride the smart-filter machinery as a follow-up.
- [x] **Settings/Help stubs** — done 2026-07: all five former stubs are wired (and, having actions now, all appear in the `Ctrl+K` command search). **Settings ▸ Performance…** and **Scratch disks / storage…** deep-link straight into the Preferences dialog (new `initialTab`/`onTabChange` props; plain Preferences… reopens on the last-visited tab). Scratch disks opens a new **Preferences ▸ Storage** tab — the honest browser equivalent: origin storage estimate (used vs available, meter, via `navigator.storage.estimate()`), **persistent-storage** status + request button (protects autosave/recents from eviction), and a per-store breakdown with confirm-guarded clears — autosave snapshot (docs/~size/timestamp → Delete), recent files (count → Clear), saved presets & swatches (gradients/swatches/adjustment looks/export presets → Delete; settings reset stays with the footer's Restore defaults). Helpers in new `app/lib/storage.ts`. **Help ▸ Getting started / Documentation** open a new **Help window** (`HelpDialog.tsx`): Preferences-style topic nav with a 7-step quick-start guide plus ten hand-curated reference topics (tools, selections, layers & masks, adjustments & colour, filters & effects, text & vector, open/save/export, colour management, view & workspace, storage & privacy) in `app/lib/help.ts`, searchable across all topics (entries carry optional shortcut chips). **Help ▸ About** (`AboutDialog.tsx`): logo, version from `package.json`, format matrix, local-only privacy note, cross-links to the guide and shortcuts. Keyboard Shortcuts was already done. Honest notes: help content is hand-curated (new features need a matching entry — it is NOT generated from code), and the §10 onboarding overlay tour + sample document remain separate future work.

## 2. Tools

**P1**
- [x] **Polygonal lasso + magnetic lasso** — done 2026-07: the Lasso tool now has a **Mode** option (Freehand / Polygonal / Magnetic; `Shift+L` cycles, persisted in tool prefs, segmented control in the options bar with distinct icons). **Polygonal** is click-to-drop-vertices with a live rubber-band edge to the cursor and vertex handles (accented start dot); closes on **double-click**, a click back on the start point, or **Enter**; **Backspace** removes the last vertex, **Esc** cancels — and switching tool/mode cancels an open polygon. **Magnetic** — REWORKED 2026-07 into the pure, Node-verified [magnetic.ts](app/lib/magnetic.ts) (17 synthetic-scene checks in scratchpad verify-magnetic.ts; CanvasArea only does readback/scaling/per-stroke state). Edge detection: **per-channel Sobel (max of R/G/B)** so isoluminant colour boundaries — invisible to the old luma-only map — snap like any edge (verified: matched-luma red/green, Δluma < 1, still reads ≥ 100 strength); channels are **3×3 box-blurred first** (noise/texture suppression — 20/20 snaps land on the true edge through ±22 uniform noise) and magnitudes **normalize to the image's own 99th percentile** (soft-but-dominant edges clear the threshold; flat images stay flat — no noise amplification, verified) with the gradient **direction stored per pixel** (folded 0–180°). Snap search: **zoom-aware reach** — ~28 SCREEN px regardless of zoom (clamped 10–96 doc px), so a subject outlined at fit-to-screen pulls from just as far as at 100% (the old fixed 30 doc px shrank to ~7 screen px at 25% zoom); **anisotropic band scoring** (sideways-to-travel candidates cost 0.5/px, ahead/behind 1.2/px — pulls to the contour beside the path instead of lunging at edges crossing ahead, verified); **continuity** (candidates pay 0.6/px beyond the cursor's own step distance from the previous snapped point, capped so decisive moves still escape — parallel-contour theft and escape both verified); and **orientation coherence** (±14 score by folded-angle agreement with the previous point's gradient — resists hopping onto perpendicular edges). Weak-edge fallback to the raw cursor retained (threshold 18, normalized). Both reuse the existing `engine.lassoSelect` rasterizer + `combineSelection` (Ctrl-add / Alt-subtract), so add/subtract, marching-ant tracing and the whole selection pipeline work unchanged. Follow-up ideas: options-bar tunables (reach/frequency), anchor-point insertion (auto-drop periodic anchors), full live-wire (Dijkstra shortest-path along the cost field) for hands-off tracing between anchors.
- [ ] **Smudge** and **Sponge** (saturate/desaturate) brushes — the coverage-mask session model from Blur/Dodge fits directly.
- [ ] **History brush** — paint from a chosen history state's snapshot.
- [x] **Red-eye** tool — done 2026-07 (`app/lib/redeye.ts` pure math + `engine.redEye`; tool `Y`, Eye icon, beside Spot Heal). One click fixes one eye: a **redness heuristic** (R − max(G,B)) finds the pupil blob within the search ring, **re-centres on the blob's weighted centroid** (forgiving clicks), then neutralizes by **replacing R with min(G,B)** — which inherently preserves the white catchlight (it isn't red, so its membership is 0) — and darkens toward a natural pupil (Darken option 0–100). Two robustness rails caught by the tests: membership thresholds are **adaptive to the blob's peak redness** (skin at redness ≈50 never qualifies next to a ≈150 pupil), and a **peak floor (70)** makes clicks on plain skin a provable no-op (no history entry either); no mask blur — feathering comes from the redness ramp + radial falloff, since blurring bled the fix into catchlights. Options bar: Size (ring cursor on the overlay, like the other brushes) + Darken, persisted in tool prefs; leaf-layers only; **respects an active selection** (coverage-clipped like heal); single "Red Eye" undo step. Verified: 16 numeric checks on a synthetic eye (pupil neutralized+darkened, catchlight byte-survives, in-radius skin untouched, off-centre click recovery, blue/skin no-ops with unchanged bytes, darken monotonicity, edge clamping, transparent-pixel skip); tsc clean.
- [ ] **Measure/ruler tool** — distance + angle readout in the status bar; feeds Straighten.
- [ ] **Perspective crop** — crop with a quad, resampling to a rectangle.

**P2**
- [x] **Spot heal brush** — done: paint a blob (`J`), it heals on release (auto source-patch search + diffusion membrane tone-match in `heal.ts`). Follow-ups: a sourced healing-brush variant (Alt-pick source like Clone) and a Patch tool (drag a selection to its source).
- [x] **Content-aware fill** — done: Edit ▸ Content-Aware Fill (`Shift+F5`) synthesizes the selection from feather-blended source blocks + one membrane pass. Follow-up: PatchMatch-style iteration for very large/structured fills.
- [ ] **Quick Selection / Object-ish select** — brush that grows a selection along edges (graph-cut/flood with edge cost; no ML needed for a solid v1).
- [ ] **Mixer brush** (wet paint blending) with brush texture/dual-tip options.
- [ ] **Custom shape tool** — user shape library (SVG path import → shape presets).
- [ ] **Direct-select / path-edit tool** as a first-class tool (pen paths exist; editing after commit is limited).
- [ ] **Frame/placeholder tool** and **Artboards** (multiple canvases per document).

**Tool options (P1 sprinkles)**
- [ ] Brush: **pressure dynamics** (size/opacity/flow from pointer pressure — `PointerEvent.pressure` is already available), scattering, angle/roundness, dual brush, texture; brush preset picker with previews; `[`/`]` size shortcuts + on-canvas HUD (Alt-right-drag size/hardness).
- [ ] Eraser: "erase to history" option.
- [ ] Move: auto-select layer/group toggle, alignment buttons (align/distribute selected layers), transform handles always-on option.
- [ ] Text: full character panel options in the bar (tracking, leading, baseline, all-caps), font search with previews, recent fonts.
- [ ] Gradient: dither option, reverse in options bar (exists in editor), noise gradients.
- [ ] Crop: delete-vs-hide cropped pixels, content-aware fill for straighten gaps.

## 3. Selections & masking

- [ ] **Quick Mask mode** (paint a selection as a red overlay; `Q` toggle).
- [ ] **Save / load selections** (named alpha channels in the Channels panel; serialize in `.gproj`).
- [ ] **Refine edge** dialog — smooth/feather/contrast/shift-edge with preview on white/black/overlay; decontaminate colours.
- [ ] **Border / Smooth / Expand / Contract** under Select ▸ Modify (Grow/Feather exist).
- [ ] **Transform selection by numbers** (x/y/w/h/angle fields in the options bar).
- [ ] **Vector masks** (a path as a mask alongside the raster mask).
- [ ] Feather **preview** (marching ants show the soft edge as a second faint outline).

## 4. Layers

- [ ] **Lock flags** — transparency, pixels, position, all (checkboxes in the panel header like PS).
- [x] **Colour labels + layer search/filter** — done 2026-07. **Colour labels**: `label?: LayerLabel` on every node kind (7 colours — red/orange/yellow/green/blue/purple/gray, registry + `labelColor()` in layers.ts), set from a **swatch row in the layer context menu** (applies to the whole multi-selection; ✕ clears), shown as a small dot before the layer name, serialized in **`.gproj` v10** (additive — older files open unchanged; loader passthrough in all three remap branches). **Search/filter row** at the top of the Layers panel: a name input (case-insensitive substring, Esc clears, keystrokes stopped from tool shortcuts) + a kind select (All/Layers/Groups/Adjustments) + a **colour-dot strip** that appears once any label exists (dots toggle, multiple labels OR together; all three criteria AND). Filter resolution is the PURE `filterLayerTree` in layers.ts (18 Node checks in scratchpad verify-layerfilter.ts): matches show, **ancestors of matches stay visible but render dimmed** (data-dim 0.45 opacity) so hierarchy stays readable, **a matching group reveals its subtree**, and matches inside collapsed groups are force-revealed so a search always shows its hits. **Drag-reorder is disabled while a filter is active** (reordering with parts of the tree hidden would be blind — the tooltip-free guard simply ignores dragover; clear the filter to rearrange). Distinct "No layers match" empty state. Honest scope notes: label changes follow the panel's live-patch convention (no history step, same as rename/opacity); the filter is display-only (never touches the tree; selection/rename/menu actions work on filtered rows); **lock flags** from the same P1 bundle remain open (§4 line above).
- [ ] **Linked layers** (move/transform together without grouping).
- [ ] **Fill layers**: Solid Colour / Gradient / Pattern as re-editable layer kinds (pairs perfectly with masks + clipping).
- [x] **More adjustment types** — done 2026-07 (`app/lib/adjust-extra.ts` + `AdjustmentExtraDialog.tsx`): **all six** as first-class adjustment-layer nodes in the `AdjustmentSpec` union (serialize verbatim in `.gproj`, honour opacity/blend/mask/clip, cache via the render graph). **Hue/Saturation with per-range targeting** — Master + the six colour ranges on a feathered hue wheel (full within ±15°, fading to 45°, adjacent ranges cross-fade to exactly 1; greys are never range-targeted); sat/lightness move toward the extremes PS-style. **Selective Color** — nine ranges (six hues via chroma weights + whites/neutrals/blacks) with relative/absolute CMYK deltas. **Gradient Map** — luminance through a 256-entry LUT compiled from gradient stops, edited with the SHARED `GradientEditor` (presets included), reversible. **Channel Mixer** — per-output linear mixes (−200..200%) + constant, monochrome mode. **Color Lookup** — dependency-free **.cube parser** (TITLE/LUT_3D_SIZE 2–129/domain checks, clear errors for 1D or malformed files) + trilinear 3D-LUT application; the picked file becomes the node's name and the table rides in the spec. **Invert / Equalize** — invert is trivial; equalize does luminance-CDF equalization with colour-preserving RGB scaling. **Engine**: `applyColorMath` gained an `extra` op kind (native spaces in place; emulated Adobe RGB runs these at 8-bit via the in-place conversion pair — 16-bit twins remain a follow-up like the filter path); neutral specs skip the pass; **critical correctness guard**: Equalize is whole-image math, so BOTH region-scoped paths (dirty-rect and tiled per-tile recompute) detect it and always run the full pass — every other new type is strictly per-pixel and keeps region/tile pricing; the GPU-mixing guard is now precisely tone-only. **UI**: seven new Layer-menu entries (`adj-x-*`) AND a matching button grid in the Adjustments panel below Curves/Levels (icons per type, threaded Editor → RightDock → panel as `onAddExtra`); parameterized kinds open a live-editing dialog (Segmented range/channel pickers + bipolar sliders; one debounced undo step per gesture, shared with the tone dialogs via the widened `setToneNodeSpec`); "Edit adjustment…" in Layers/Properties routes there (Color Lookup re-opens the .cube picker); labels flow through `specLabel`/Properties. **Verified**: 45 numeric checks (invert involution, equalize CDF endpoints/no-op/alpha-skip, hue-wheel weights incl. wrap + partition, red+120°→green, per-range isolation, selective weights/relative-vs-absolute, mixer identity/swap/mono/clamp, gradient-map identity/reverse/colour stops, identity + inverting 2³ LUTs via trilinear, .cube parse + 4 error cases, neutrality flags); tsc clean. Honest notes: Hue/Sat has no Colorize mode yet; Selective Color is a documented PS approximation; extra kinds stay CPU (they're cross-channel, not per-channel LUTs, so the WebGL tone stage doesn't apply).
- [ ] **Blend-If sliders** (this-layer/underlying with split handles) and **knockout**.
- [ ] **Global light** for effects (shared angle across shadows/bevels).
- [ ] **Layer style presets** — save/import/export styles (mirror the gradient-preset system; `.astyle` files).
- [ ] **Layer comps** (named visibility/position/style states).
- [ ] **Isolate mode** (temporarily solo selected layers).
- [ ] Merge visible / Stamp visible (Ctrl+Alt+Shift+E).

## 5. Effects & filters

- [ ] **High Pass**, **Median**, **Dust & Scratches**, **Reduce Noise** (edge-preserving already exists as Surface — expose a denoise-tuned variant).
- [ ] **Lens corrections**: vignette (+/-), chromatic-aberration fix, geometric distortion (barrel/pincushion).
- [ ] **Photographic**: Dehaze, Clarity/Texture (local contrast — reuse Unsharp at large radius), Grain with size/roughness.
- [ ] **Stylistic**: Oil Paint, Halftone/Color Halftone, Crystallize (Voronoi mosaic), Glitch/Scanlines, Drop-shadow-on-canvas… as smart-filter types (the Spec 07 dispatch makes each a self-contained pure function).
- [ ] **Filter previews in the picker** (thumbnail per filter type in the Smart Filters "Add" list).
- [ ] **Warp / Perspective warp** transforms (mesh-based; shares math with Liquify).

## 6. Text & vector

- [x] **Rich text runs + justification** — done 2026-07 (letter/line-spacing UI already existed — Tracking + Leading in the text options bar). **Model**: `TextRun` (fontFamily/size/bold/italic/underline/strike/colour × len) covering the block's flat text exactly; rides on `VectorText.runs`/`TextRenderSpec.runs` (absent = uniform legacy block, `.gproj` verbatim, old files untouched); align gained **"justify"**. **Layout**: new pure `app/lib/richtext.ts` — `layoutRuns` tokenizes paragraphs into word/space tokens, splits tokens at run boundaries, greedy-wraps, gives every line the metrics of its **tallest run** (shared baseline; per-line leading = lineHeight × the line's max font size, reproducing the legacy formula exactly for uniform text), and justifies by stretching the space gaps of every wrapped line (paragraph-final lines never stretch); measurement is **injected** (canvas ctx in the app with the block's tracking as letterSpacing, a deterministic stub in tests). **Render**: engine `renderText`/`textBounds` dispatch — rich specs (runs present or justify) paint segment-by-segment with per-run font/colour/decorations (decoration spans bridge justify-stretched gaps); the uniform path is byte-stable; the no-anti-alias threshold pass covers both. SVG export emits per-segment positioned `<text>` elements from the same layout. **Editor**: the overlay `<textarea>` became an **uncontrolled contentEditable div** — seeded once per session from {text, runs} (spans only for overrides), the browser owns caret/selection/typing/undo, plain-text-only paste, Enter → `insertLineBreak` (uniform `<br>` newlines, no nested blocks), and serialization walks the DOM with **computed styles** (nested spans, execCommand output and pasted-then-styled content all resolve; decorations accumulate down the tree; NBSP normalized; the trailing placeholder `<br>` dropped). **Selection styling**: options-bar changes route through a new `ViewApi.applyTextStyle` — with a non-collapsed selection in the editor, character-level patches (B/I/U/S via native `execCommand` toggles, colour/font via `styleWithCSS`, px font-size via manual range-wrap) style the RANGE and the base style stays put; with a bare caret they set the block/base style as before. Ctrl+B/I/U inside the editor are selection-aware the same way; Ctrl+Shift+J = justify; the align control gained the Justify segment. **Gotcha recorded**: CSS `text-decoration` paints through descendants and cannot be cancelled by a child — so the editor's base style NEVER carries underline/strike; runs own their decorations as spans. **Verified**: 29 numeric layout checks (normalize clip/pad, legacy-formula baseline reproduction, point/box alignment anchors, wrap + trailing-space/wrap-gap rules, run-split words, tallest-run line metrics, justify edge-exactness + paragraph-final + newline exemptions, blank-line heights); tsc clean. Honest limits: the editor overlay's own wrap can differ a hair from the raster for exotic cases (same as the old textarea); per-run tracking/leading not modelled (block-level, like before); no OpenType features (separate TODO).
- [x] **OpenType features + variable axes** — done 2026-07-20, exactly as scoped ("via canvas where supported"). A **Ligature-icon popover** in the text options bar (body-portalled like the layer context menu — the bar's masked scroll container clips fixed descendants) with ten **block-level feature toggles** (standard/discretionary ligatures, contextual/stylistic alternates, small caps, oldstyle/tabular figures, fractions, slashed zero, stylistic set 1) and **variable axes**: a Weight slider (100–900, overrides Bold; ✕ returns to the toggle) and a Width select. **The two canvas truths this is built on (probed empirically in Chromium, encoded in [richtext.ts](app/lib/richtext.ts) helpers): (1)** `font-feature-settings` reaches canvas text only as CSS on the canvas ELEMENT and only while it's CONNECTED — so `renderText`/`textBounds` mount the (normally detached) layer/measure canvas into a shared `display:none` host for the draw when features are non-default (`withTextFeatures` in paint.ts; default features skip all DOM work, keeping legacy text byte-stable); **(2)** `font-variation-settings` NEVER reaches canvas, but the font SHORTHAND maps `font-weight` (any 1–1000) and `font-stretch` keywords onto a variable font's wght/wdth axes even detached — so axes ride `cssFontString` everywhere fonts are set (uniform, rich runs, measurement, SVG export). `features`/`axes` live on TextSettings/VectorText/TextRenderSpec (additive — old `.gproj` files and specs untouched), survive re-edit seeding, style the contentEditable overlay (same quantized stretch keyword as the raster, so the editor never lies), and **SVG export** emits font-weight/font-stretch/font-feature-settings with its measure canvas mounted+styled the same way so exported layout matches the raster. FONT_FAMILIES gained Calibri, Bahnschrift and Segoe UI Variable so features/axes bite out of the box on Windows. **Verified end-to-end on the raster** (headless Edge): Calibri renders true small caps, Bahnschrift renders visibly light at wght 200 and black at 900, popover defaults (liga/calt on), Esc/backdrop close, zero console errors; plus 3 standalone probes pinning the platform behaviour. **Honest limits**: features/axes are **block-level** (not per-run — same boundary as tracking/leading); Width quantizes to the nine CSS stretch keywords (the shorthand takes no percentages); no slant/optical-size/custom axes (canvas rejects `oblique <angle>` in the shorthand); toggles apply only where the font actually has the feature (no per-font feature DETECTION — that needs table parsing); and **custom font loading via `FontFace`** (the natural next step this mechanism already supports) still has no import UI.
- [ ] **Text on a path** and **warp text** presets (arc, flag, bulge).
- [x] **Paths panel** — done 2026-07: a new right-dock **Paths** panel (Spline icon, Window ▸ Paths, default hidden) storing pen paths PER DOCUMENT, with the model + geometry in the pure [paths.ts](app/lib/paths.ts) (17 Node checks in scratchpad verify-paths.ts). **Store**: every Pen-tool commit auto-lands as the Photoshop-style **Work Path** (italic, replaced on each commit — CanvasArea's `finishPenPath` hands the anchors up via `onPenPathCommit` BEFORE clearing); **Save** duplicates it as "Path N" (renamable via double-click, deletable); rows show a live **SVG thumbnail** (`pathToSvgD` — M/C/Z data over a `pathBounds` viewBox, no canvas management). **Reuse**: **→ Selection** samples the cubics into a polygon (`samplePathPolygon`, same bezier math pen.ts strokes; open paths auto-close with a straight chord like the lasso) and rasterizes through the existing `engine.lassoSelect` — with the lasso's own modifier conventions as the **boolean combines**: plain click = replace, **Ctrl = add, Alt = subtract, Ctrl+Alt = intersect** (intersect built as A − (A − B) from the existing subtract; all journaled via `commitSelection`). **→ Stroke** replays the path through the engine's `livePath`/`endPath` (new `strokePath` EngineHandle verb) with the Pen tool's current width/taper/bend + the foreground colour — one tight undoable "Path" step, identical to committing the pen tool. **→ Fill** fills the rasterized region with the foreground colour via `fillSelection`. **Edit** loads the stored anchors back into the Pen tool (`ViewApi.loadPenPath` — deep-copied, commits any in-progress path first, re-targets the active layer). **Persisted in `.gproj` v11** (`paths` on the file; `coercePaths` garbage-tolerant load; older files open unchanged). Honest limits: boolean combines act at the SELECTION level (rasterized) — true bezier path booleans are out of scope; fills/selections are hard-edged like every selection fill; stroking/editing targets the ACTIVE layer at commit time.
- [x] **SVG import + SVG export** — done 2026-07 (`app/lib/svg.ts`, dependency-free). **Import**: `.svg` files picked in File ▸ Import parse through the browser's own machinery — DOMParser + a hidden **closed-shadow-root mount** (the file's `<style>` can't leak into the app), so `getScreenCTM` resolves every transform/viewBox nesting and `getComputedStyle` resolves the full CSS cascade (classes, inheritance, presentation attrs). Graphic elements (path, rect incl. rounded, circle, ellipse, line, polyline, polygon, nested groups) become a **`VectorPath` recipe** — a new third `VectorData` kind: styled subpaths (`d` + element→doc matrix + fill/fill-rule/stroke/width/cap/join/miter/dash, per-paint opacity baked into #rrggbbaa) that `Path2D` re-renders crisply at any time. The layer's PIXELS are rendered **from the recipe**, so they can never disagree with it; placement (import anchor / own-canvas) folds into the matrices. Recipes ride `.gproj` automatically, duplicate with the layer, and show as "Vector layer" in Properties; the Import dialog notes which items import as vectors. **Honest fallback**: files using anything beyond the subset — text, `<use>`, `<image>`, gradients/patterns (`url()` paints), filters, masks, clip-paths, markers, translucent multi-child groups (isolation), both-paints element opacity — import as a faithful **raster** of the browser's own rendering (blob-URL `<img>` decode) with **no** vector recipe, rather than a silently degraded vector. **Export**: File ▸ **Export SVG (vector layers)…** serializes visible vector-bearing layers bottom→top — imported vectors as `<path matrix()>`, **shape layers via the exact ring geometry `renderShape` rasterizes** (outer/inner rounded outlines, evenodd stroke ring, reusing shapes.ts `triPoints`/`trapPoints`/`insetPoly`; ellipse maps to a native centred-stroke `<ellipse>`), **text layers via the same line layout `renderText` uses** (canvas-measured greedy wrap, alignment anchors, leading/tracking, underline/strike) — with group nesting, layer opacity, and CSS-mappable blend modes (`mix-blend-mode`); raster/adjustment layers are skipped and counted in the result toast, and an all-raster document gets a clear alert instead of an empty file. **Verified**: 44 numeric checks on the pure parts (path-data builders incl. arcTo-equivalent corner rounding + sweep flags, computed-paint→hex8 conversion, recipe translation immutability, and the full export serializer: ring/fill structure, rotation pivot, ordering, escaping, opacity/blend/dash/caps, skip counting); tsc clean. Known limits (documented): imported recipes aren't yet interactively re-editable (needs the §2 direct-select/path tool) and — like the existing shape/text recipes — don't rescale with Image Size; group-opacity isolation and gradients are exactly what trigger the raster fallback; SVG text export references fonts by name (viewer must have them).

## 7. Colour management & depth

- [x] **ICC profile import + assign-vs-convert** — done 2026-07: a dependency-free ICC reader (`app/lib/icc.ts`) sniffs embedded profiles from the raw file bytes — PNG `iCCP` (zlib via `DecompressionStream`), JPEG `APP2 ICC_PROFILE` (segments re-assembled by sequence), WebP RIFF `ICCP` — and parses the profile's display name from the `desc` (ASCII) or `mluc` (UTF-16BE, v4) tag, classifying it against the working space. When an import carries a profile that ISN'T the working space, the Import dialog shows a **Color profile** section naming it, with the classic choice: **Convert to working space** (recommended — the browser's colour-managed decode, keeps appearance) or **Assign working space** (re-decode with `colorSpaceConversion: "none"` — keeps the raw numbers, ignores the tag). Verified with synthetic PNG/JPEG containers for both tag types. Honest limits noted: AVIF/HEIF/TIFF boxes aren't sniffed (their decodes still colour-manage as before), clipboard pastes stay convert-implicit (the clipboard hands over decoded bitmaps), and the profile bytes are kept on the item for a future export round-trip but not yet re-embedded on export.
- [x] **Soft-proofing** — done 2026-07: **Proof colors** (`Ctrl+Alt+Y`) simulates a target space on the VIEW only, and **Gamut warning** (`Ctrl+Alt+Shift+Y`) paints out-of-gamut pixels Photoshop-style mid-grey; both in the View menu (checkable) and the Color Management dialog, where the target (sRGB / Display P3 / Adobe RGB, persisted) is chosen. The transform (`proofTransformInPlace` in colorspace.ts) decodes the canvas bytes to linear, converts through the target's primaries (P3 matrices added; D65 throughout), flags out-of-gamut against a half-byte-step epsilon, clips in target-linear (that clip IS the simulation) and converts back for display — in-gamut pixels are byte-identical, and identity combinations (sRGB canvas → any larger target) skip entirely, which the dialog explains. Hooked at the engine's view blit, reusing the dirty-rect bound so painting under a proof stays region-priced; exports and `exportComposite` never proof. Verified numerically: P3 grey/muted tones pass through byte-exact, saturated P3 red warns grey, and its simulation clips to (234, 51, 35) — exactly sRGB red in P3 coordinates. Honest limits: targets are the three standard spaces (no arbitrary ICC rendering intents — no CMM), and proofing operates on the CANVAS space (so under the emulated Adobe RGB working space it proofs the sRGB canvas).
- [x] **More working spaces** — done 2026-07: **Adobe RGB (1998) as an emulated working space** (`app/lib/colorspace.ts`), exactly as scoped — manual matrix conversion in the adjustment pipeline, limits documented in the Color Management dialog. Storage/display stay on an sRGB canvas (browsers can't display or export an Adobe RGB canvas); the adjustment pipeline — slider adjustments, adjustment layers, Curves/Levels, including the region-scoped partial path — converts sRGB→Adobe (exact D65 matrices computed at load from the published primaries; Adobe gamma 563/256; per-byte decode LUTs, `Math.pow` encode, single-rounding via `Uint8ClampedArray`), runs the math in Adobe primaries, and converts back. Verified: neutrals and in-gamut colours roundtrip byte-exact, sRGB red → Adobe (219,0,0) matches the published reference; extreme saturated colours quantize ±≤5 per pass (inherent to any 8-bit space conversion — the 16-bit pipeline item is the real fix). Toggling to/from Adobe RGB is lossless (no pixel conversion; only the math redirects; adjustment products recompute via the epoch bump). The GPU tone stage defers to CPU under Adobe RGB (its LUT would apply in canvas-space bytes). Filters/effects/compositing remain in the canvas space — documented. Native spaces still convert pixels through the browser's CMS on switch, unchanged.
- [~] **16-bit/channel pipeline** (P3) — the adjustment path is done 2026-07, exactly as scoped: **Uint16 buffers for the adjustment path with the 8-bit compositing fallback**, and **tone/curve LUTs generalized to 65 536 entries** (`buildLevelsLUTs16`/`buildCurvesLUTs16` sample the SAME continuous evaluators — Fritsch–Carlson curves, closed-form levels — composed continuously, so no intermediate 8-bit rounding; byte v widens as v·257). The seam is the engine's `applyColorMath` dispatcher (all four adjustment sites: destructive sliders/tone sessions, adjustment layers, and the region-scoped partial path): native spaces keep the 8-bit path (a single pass already quantizes once); the **emulated Adobe RGB space runs fully at 16 bits** — canvas bytes decode straight to Adobe RGBA16 (`srgbBytesToAdobe16`), slider math runs in `applyAdjustments16` (kept-in-sync twin incl. the 16-bit noise/sharpen spatial tail) or the 65k LUTs, and ONE final quantization writes sRGB bytes (`adobe16ToSrgbBytes`). **Verified numerically**: the space roundtrip and identity Levels/Curves through the whole 16-bit pipeline are byte-exact over 405k samples (the old byte-intermediate path erred ±5 on saturated colours), and the 65k LUTs agree with the 8-bit tables at every byte position. Remaining (still P3): 16-bit layer STORAGE and compositing (canvas is 8-bit — the true fallback limit), the filter path (spatial kernels), and Float16/HDR sources.
- [~] **32-bit float / HDR** — the three named deliverables shipped 2026-07-18 ([hdr.ts](app/lib/hdr.ts), pure + 39-check Node suite); the canvas itself stays SDR (see open tail). **Merge to HDR** (File ▸ Merge to HDR…, [HdrMergeDialog](app/components/HdrMergeDialog.tsx)): load 2+ bracketed exposures (decoded to sRGB; same pixel size required, tripod-aligned — NO auto-alignment), per-frame **EV auto-read from EXIF** (`log2(N²/t) − log2(ISO/100)` off the metadata reader's raw twins; when any frame lacks capture data, all frames get brightness-ranked 2-stop-spaced estimates) and hand-editable; merge is Debevec-style — per pixel, each frame's sRGB-linearized value scales by `2^(ev − evRef)` and averages under a luma hat weight, frames with ANY clipped channel (≥254) are excluded at that pixel (their colour is wrong), and fully-clipped pixels fall back to the darkest frame (verified: <1% worst relative error on synthetic ±2EV brackets; blown highlights recover from the darkest frame; super-highlights clamp at the bracket ceiling instead of producing garbage). **Exposure slider + tone mapping**: live preview (block-averaged float downscale), exposure ±4 ev, three operators — Hable **filmic** (normalized to white 11.2), **extended Reinhard** on luminance (colour-ratio-preserving), **linear** clip — rendered into a NEW document; the Float32 radiance map stays on the doc IN MEMORY, and **Image ▸ HDR tone mapping…** re-renders the active layer from float any time via the new `applyLayerImage` engine verb (ONE undoable whole-layer pixel entry — repeated tone edits never stack 8-bit loss). **PQ/HLG export** (File ▸ Export HDR PNG…): a hand-written **16-bit/channel PNG encoder** (zlib via `CompressionStream("deflate")`, chunk CRCs from zip.ts, all verified structurally + against a Node-zlib pixel oracle) tagged **Rec.2100 via cICP** (primaries 9/BT.2020 with the BT.2087 709→2020 matrix, transfer 16 PQ or 18 HLG, full-range), SDR white at 203 nits, PQ peak clamp 400–4000 nits, extra exposure control; the dialog reports display capability via `matchMedia("(dynamic-range: high)")`. Fixed along the way: CanvasArea's `setLayerImage` pass-through dropped the x/y placement args (offset imports landed at 0,0). Still open (the honest tail): handheld-bracket **auto-alignment**, **float persistence** in .gproj (the map is session-only — a 12MP doc ≈ 144 MB of float, needs a binary/half-float container), true **HDR canvas display** (browser HDR canvas is still experimental), and HDR AVIF (no encoder control over bit depth/CICP from Canvas).

## 8. Engine & performance

- [x] **Web Worker offload** — done 2026-07 across all three heavy paths:
  - **Blur Gallery**: dedicated module worker caches the session's originals once, per-tick param messages, sequence/session-guarded replies, sync fallback (`app/workers/blurfx.worker.ts` + `previewBlurFxAsync`).
  - **Heal / content-aware fill** (`app/workers/heal.worker.ts`): the membrane solve runs off-thread; replies are validated against the document state captured at post time (epoch / dimensions / layer existence) before baking + one history entry; a worker error finishes any in-flight jobs synchronously so no released blob is lost.
  - **Smart filters** (`app/workers/filters.worker.ts`): the full stack pipeline (filters bottom→top, per-filter blend/opacity via OffscreenCanvas, filter-mask premultiplied lerp) with a **stale-while-refresh product cache** in the engine (`filteredProduct`) — no async compositor needed: on a key miss with a previous product available, the stale product draws this frame while the worker recomputes; the reply lands in `filteredCache`, clears the render cache (frames in flight cached stale products under fresh keys) and recomposites. Live paint sessions on filtered layers bypass the cache (in-line compute, exactly as before); cold first render and "Apply (bake)" stay synchronous for correctness. Known tradeoffs: worker jobs are posted per drag tick (serial worker queue can lag long drags; newest-per-node reply wins), and the preview trails the slider by ~a tick like the Blur Gallery.
- [~] **WebGL/WebGPU renderer** (P3) — the highest-value stage is done 2026-07: a **WebGL2 tone-LUT renderer** (`app/lib/gpu.ts`) behind the render-graph seam. Curves/Levels adjustment layers now skip BOTH the full-document `getImageData` readback and the JS per-pixel loop: the below-accumulator canvas uploads straight to a texture, a 256×1 NEAREST LUT texture maps each channel exactly (`(v·255+0.5)/256` sampling → exact bucket), and the result returns via GPU→GPU `drawImage`. Correctness rails: sources upload unpremultiplied (getImageData semantics) into a `premultipliedAlpha:false` context; display-p3 documents use `drawingBufferColorSpace`/`unpackColorSpace` where supported and fall back to CPU otherwise; context loss → permanent CPU fallback; tone products are never mixed-source (the region-scoped partial patch defers to the GPU full pass, which is cheap); `exportComposite(clean)` forces the CPU path for byte-certain output; A/B via `window.__gqGPU.enable()/disable()/status()` (dev), mirroring `__gqRenderCache`. **Deliberately NOT GPU** (and why): layer blend modes already run on the GPU through Canvas2D `globalCompositeOperation`; the slider-adjustment math (`applyAdjustments`) is a large hand-written colour pipeline where a GLSL port would risk divergence for modest gain; effects/smart-filters have kernel reach and live in the worker path instead. A full GPU compositor (or WebGPU compute for filters) remains open as the v2-engine step.
- [x] **Dirty-region recompute inside nodes** — done 2026-07 for the case that matters: **region-scoped adjustment read-backs**. Every cached adjustment product now tracks the union rect of committed changes beneath it (`adjMeta`; `pushEntry` passes its patch rect into `bumpPixel`, so brush/heal/fill commits AND undo/redo are rect-bounded). On a key miss where the same immutable tree is live, the adjustment's own key parts are unchanged and every change since caching was rect-bounded, only that rect is re-read, re-processed (same math via `applyAdjustmentRegion` — adjustments are strictly per-pixel, so this is byte-exact) and re-modulated; outside it the old product is reused. Guarded by `changeReaches`: changes on layers with enabled effects/smart filters (or under styled ancestor groups) spread pixels beyond their rect and force the full path — this check also fixed a latent partial-BLIT hazard (undoing a stroke on a shadowed layer could leave a stale shadow on screen outside the patch rect). Not attempted (still full-size on miss): effects and smart-filter recompute regions — both have kernel/offset reach and need padded-region math.
- [x] **Tiled compositing** — done 2026-07 for the case the item names ("so caches/evictions work per-tile"): **adjustment products on very large documents are cached as 1024-px tile grids** (engaged when one full product would reach 64 MB, the smallest budget setting — ≈4k×4k, so every ≥ 8k document; an 8k² product is ~268 MB, larger than the entire default budget). **Per-tile eviction**: the LRU evictor sees each resident tile as its own candidate (`selectEvictions` loosened to `{bytes, tick}` entries; tiles of frame-used products stay protected), so an over-budget cache sheds exactly the overage instead of whole products, and a freed tile is later recomputed **alone** from the below-accumulator. **Per-tile recompute**: the dirty-rect proof (adjMeta) now marks only the tiles under a committed rect stale — replacing the old region path's per-commit full-canvas copy + 268 MB allocation with a few tile-sized ops — and, new with this change, **mask paints on the adjustment itself are region-priced too** (the tiled own-signature excludes the mask version; the mask rect rides the same dirty bookkeeping, sound because modulation is per-pixel). GPU tone products never mix with CPU tiles: any staleness under an active GPU pass reruns the (readback-free) full pass and re-slices all tiles from that single source. Guardrails intact: `exportComposite(clean)` and the `__gqRenderCache` A/B disable bypass tiling wholesale; group-local accumulators stay uncached; resize/crop/space changes clear through a now-centralized cache reset (which also fixed a latent leak — the filter-worker error path cleared the cache without resetting the byte count). Verified: 32 numeric checks on the pure tile math + eviction selection (partition exactness incl. edge tiles, thresholds, tile-vs-product LRU order, pseudo-id round-trip, protection); tsc clean. Deliberately NOT tiled: leaf/group/clip products (effects/smart filters have kernel reach — a tile can't recompute in isolation without padded-region math) and the per-frame compositor itself (Canvas2D draws are already GPU-side; viewport-culled tile compositing is v2-engine territory with exportComposite/histogram implications).
- [x] **Progressive preview** — done 2026-07 for smart-filter drags on large documents (≥ 2 MP): a param drag kicks the worker at **half resolution** — the source downscales on the GPU before the readback (¼ the pixels AND ¼ the `getImageData` cost), spatial filter params scale with it (`scaleFilterParams`: blur px, unsharp radius, mosaic cell, wave amplitude/wavelength, emboss height — %/degree/threshold params untouched) and the reply upscales smoothly to document size. **Settle is timer-free**: it falls out of the composite loop — when a composite hits a preview-quality product whose key still matches (params stopped moving), a full-res refinement job kicks quietly and swaps in. Exports are protected engine-side: `exportComposite` never accepts preview-quality or stale-param products (sync full-res unless the cache is an exact full-quality hit), and the same fix makes the `__gqRenderCache` A/B toggle and clean exports bypass the filtered-product cache entirely. Not extended to the Blur Gallery worker (its session-cached originals would need scaled copies) — its drags are already non-blocking.
- [x] **Cache budget in Preferences** — done 2026-07: Preferences ▸ **Performance** tab with a 64–1024 MB budget slider (`prefs.cacheBudgetMB`, applied via `setRenderCacheBudget`, evicts immediately when shrunk — frame-protected entries excluded) and **live statistics** polled each second while the tab is open: memory used vs budget (with a usage meter), cached-product count, session hit rate (new `cacheHits`/`cacheMisses` counters in `renderNode`), and the debug on/off state.
- [x] **Autosave / crash recovery** — done 2026-07: periodic `.gproj` snapshots to IndexedDB on a Preferences interval (default 2 min, only when history moved), a `pagehide` heartbeat flag that detects an unclean exit, and a Restore/Discard prompt on the next boot; the status bar shows the real save state ("Unsaved changes" / "Saved" / "Autosaved HH:MM"). **Extended 2026-07 to ALL open documents** (was active-tab only): the snapshot now stores every tab's `.gproj` JSON + the active index. Works because the engine retains every *materialized* document's layer + mask canvases simultaneously (globally-unique ids; `setDoc`/`transformMasks` skip non-owned layers; no clear on switch), so any tab serializes on the fly via the engine's global store. A per-document JSON cache is refreshed when a tab is active (each tick) and when you switch away from it (it can't change while inactive), seeded from a tab's file/snapshot JSON on load so a never-viewed tab (pixels still queued) still restores; closed tabs are pruned. Restore reopens every document and activates the one that was active. The IndexedDB format migrates a legacy single-doc entry transparently. Verified: 9 migration/round-trip checks (multi-doc, legacy migration, activeIndex clamp, junk→null); tsc clean.
- [ ] Perf HUD (dev): composite ms, cache hit rate, dirty rect overlay.

## 9. File formats & import/export

- [~] **RAW support** (P3) — the hand-written decoder is done 2026-07: a dependency-free **DNG subset decoder** (`app/lib/dng.ts`, ~650 lines) covering the TIFF/IFD structure (both byte orders, SubIFD walking, NewSubfileType-scored raw-IFD selection), **uncompressed** raw (16-bit words and MSB-packed 10/12/14-bit rows) and **lossless JPEG** (ITU-T81 SOF3: canonical Huffman DC tables, all 7 predictors, point transform, FF-stuffing, multi-component scans) in strips or tiles, CFA patterns up to 8×8, LinearRaw, then the develop pass: per-cell BlackLevel/WhiteLevel normalization → **as-shot white balance** (AsShotNeutral, green-normalized) → **camera→sRGB matrix** (ColorMatrix2/1, dcraw-style row normalization + inversion) → BaselineExposure → sRGB gamma, with **bilinear demosaic**, ActiveArea + DefaultCrop, and orientations 3/6/8. Runs in a throwaway worker (`dng.worker.ts`) so a 24 MP decode never blocks the UI; wired into `decodeImageFile` ahead of the browser decode for TIFF-shaped files, with the embedded-JPEG-preview fallback intact for everything outside the subset (lossy-JPEG/JXL DNGs, float HDR, non-DNG RAW containers). **Verified against synthetic DNGs built by the test itself** (a TIFF writer + a lossless-JPEG encoder): the uncompressed and lossless-JPEG paths decode byte-identically, a grey card develops to the exact analytic value, and the tests caught a real SOS-parse bug. Remaining (still P3): an interactive develop dialog (WB/exposure sliders before import), feeding a 16-bit layer (blocked on §7 16-bit storage), Malvar-He-Cutler demosaic, and other RAW containers (CR2/NEF/ARW stay preview-only — or the explicit wasm-libraw decision).
- [x] **PSD** — done 2026-07 (`app/lib/psd.ts`, ~750 lines, dependency-free, fully Node-testable — no DOM anywhere). **Import** (File ▸ Import, `.psd` opens as its own layered document via the same PendingLoad machinery `.gproj` uses): PSD v1, 8-bit RGB/Grayscale; layers with RAW + **RLE (PackBits)** channel data, **nested groups** (`lsct` section dividers, bottom→top stream rebuilt into the top-first tree with a stack), **user layer masks** (rect + default-colour expanded to doc size, disabled flag honoured), opacity/blend/**clipping** (maps to the app's clipping masks)/visibility, unicode (`luni`) names, resolution → document ppi. **Text and smart objects arrive as their rasterized pixels** (PS stores those in the channels). Adjustment/fill layers are skipped with a note (19 adjustment keys detected); "pass-through" group blend reads as Normal silently. **Honest fallback**: 16/32-bit, CMYK or layer-less files import as the **flattened composite** when readable (single Background layer + note); PSB rejected with a clear alert. **Export** (File ▸ Export PSD (layered)…): PSD v1 8-bit RGB — the full tree as section dividers, masks (with enabled flag), opacity/blend/clipped/hidden, pascal + `luni` names, **RLE-compressed channels**, a ResolutionInfo resource carrying the ppi, and the required **flattened composite** (RGB over white) so layer-ignorant readers still show the document; adjustment layers skipped + counted in the toast (no PSD equivalent for our specs — the composite keeps the look); effects/smart filters don't bake into per-layer pixels (noted). **Verified: 26 numeric checks with the reader as the writer's oracle** — PackBits identity (runs >128, long literals), full write→parse round-trip (structure, nesting, unicode names, hidden/clipped/disabled-mask flags, opacity quantization, byte-exact pixels AND masks, empty layers), composite fallback incl. white matting, all 18 blend modes, garbage rejection; tsc clean. **Bug worth remembering**: `r.pos += r.u32()` clobbers the read's own position advance (JS evaluates the left operand first) — cost an hour of byte-level tracing. Not attempted: 16-bit channels, layer effects ↔ PSD styles, editable text, ICC embedding, PSB; parsing runs on the main thread (a worker like DNG's is a follow-up for very large files).
- [x] **TIFF + HEIF** — done 2026-07-18. **TIFF codec** ([tiff.ts](app/lib/tiff.ts), dependency-free/DOM-free, 31-check verify-tiff.ts): the DECODER covers baseline + the extensions real files use — both byte orders, **strips AND tiles** (edge clipping), **8- and 16-bit** unsigned samples (16-bit delivered as the high byte — true 16-bit storage stays with §7), grayscale both polarities, **palette** (u16 colormap), RGB/RGBA with **associated-alpha un-premultiply**, compressions **none / PackBits / LZW / Deflate** with **predictor 2** (8- AND 16-bit differencing in file byte order), orientations 3/6/8, XResolution→dpi, multi-IFD files (first full-res image wins; NewSubfileType-reduced IFDs skipped). Outside the subset → null → the import falls back to browser/preview decode (JPEG-in-TIFF, CCITT, float/signed, planar, bilevel). **Verified against a REAL codec**: a GDI+-written 200×200 LZW TIFF decodes pixel-exact (this oracle caught an LZW early-change fencepost the synthetic round-trips couldn't — the reference encoder and decoder shared the bug; the GDI+ file arbitrated the decoder as correct). The ENCODER (File ▸ **Export TIFF…**) writes little-endian 8- or 16-bit RGB(A), **Deflate-compressed ~1 MB strips** (`CompressionStream`, each strip an independent zlib stream — node-zlib verified as the decode oracle), straight alpha (ExtraSamples 2) or white-matted, ascending tags incl. resolution + Software; 16-bit is the honest v·257 widening of the 8-bit canvas (the dialog says so). Import wiring: TIFF-shaped files try DNG development first (raw wins), then this decoder — Chrome/Firefox can't decode TIFF at all, so this makes .tif a first-class import; dpi arrives via the metadata reader's existing bare-TIFF path. **HEIF/HEIC**: `.heic/.heif` added to the import accept list; decode rides `createImageBitmap` where the browser ships the codec (Safari; most others don't), and failed imports now get a **named-file alert with a HEIF-specific explanation** instead of silently dropping. Bonus: `locateTiff` gained an **ISO-BMFF branch** (ftyp scan for the contiguous `Exif\0\0`+TIFF item payload), so the Metadata panel reads EXIF from HEIF **and AVIF** files now. Not attempted: TIFF layer/page multi-import (first page only), EXIF-IFD embedding inside exported TIFFs, ICC in TIFF, float TIFF (ties to §7 HDR), and no wasm HEIF decoder (the honest scope was "where the browser decodes it").
- [~] **PDF export** — done 2026-07-18 (the print-dialog upgrades half stays open, see tail). **File ▸ Export PDF…** ([pdf.ts](app/lib/pdf.ts) + [ExportPdfDialog](app/components/ExportPdfDialog.tsx)): a hand-written, dependency-free **single-page PDF 1.4 writer** (31-check verify-pdf.ts) — Catalog/Pages/Page objects, ONE image XObject, a 4-line content stream (`cm` matrix + `Do`), a correct **xref table** (every offset verified to land on its object) and an **Info dictionary** (Title = doc name, Author = doc metadata artist → Preferences attribution fallback, Creator/Producer, timezone-correct CreationDate; strings escape parens/backslash, non-ASCII goes as **UTF-16BE hex** `<FEFF…>`). **Page sizing**: "Image size" (page = pixels at a chosen ppi, 72–600, defaults to the doc's) or "Paper size" (**A4/Letter/Legal/A3/A5**, portrait/landscape, **margin slider in mm**, then **Fit to margins** — aspect-preserving, centred, upscales too — or **actual size at ppi**, centred with a ⚠ overflow warning when it exceeds the margin box; `layoutPage` is pure and unit-tested incl. the centring/overflow math). **Image embedding**: JPEG (**/DCTDecode** — the canvas-encoded JPEG bytes embed untouched, quality 60–100) or Lossless (**/FlateDecode** — raw RGB deflated via `CompressionStream`, node-zlib verified byte-exact); transparency **mattes over white** first (PDF pages are opaque, and canvas→JPEG mattes black — the pre-matte avoids both). The live note in the dialog shows the resulting page size in mm. Still open from this bullet: the **print dialog upgrades** (margins/scale/profile — browser print UI owns most of that today) and **multi-page PDF** (blocked on engines being created lazily per document — a never-activated tab has no engine to composite; needs a headless composite path first).
- [ ] **Animated**: GIF/APNG/WebP import as layer stacks; timeline-less "export frames" first.
- [x] **Metadata round-trip** — done 2026-07: exports now carry EXIF + XMP, the panel is editable, and Preferences has attribution defaults. **Writer** ([metadata-write.ts](app/lib/metadata-write.ts), dependency-free + Node-verified with the app's own reader as the oracle — 51 checks in scratchpad verify-metadata.ts): hand-written little-endian **EXIF/TIFF builder** (IFD0 + Exif IFD + GPS IFD; tags sorted ascending, inline-vs-pointer values, word-aligned value areas; description/make/model/orientation=1/X-Y-resolution from doc ppi/software/DateTime/artist/copyright; ExifVersion 0232, exposure-time + f-number + ISO + DateTimeOriginal + focal + 35mm-equiv + lens; GPS as carry-safe deg/min/sec×10⁴ rationals with N-S/E-W refs) plus a minimal **XMP packet** (dc:description/creator/rights, xmp:CreatorTool/ModifyDate — the Unicode-safe channel; EXIF ASCII is written as UTF-8, and the READER now decodes strict-UTF-8-with-Latin-1-fallback so © etc. round-trip). **Containers**: JPEG (APP1 Exif + APP1 XMP after SOI/APP0, **JFIF density patched to the doc's ppi**), PNG (eXIf + iTXt `XML:com.adobe.xmp` + **pHYs** chunks after IHDR, CRCs via the zip lib's crc32; existing pHYs respected), WebP (VP8X created-or-flag-patched with EXIF/XMP bits + canvas dims, 'EXIF'/'XMP ' RIFF chunks appended, RIFF size fixed, even-padding). AVIF passes through untouched (ISO-BMFF surgery out of scope) and ANY embed failure returns the original blob — embedding can never break an export. **Raw twins in the reader** ([metadata.ts](app/lib/metadata.ts)): dateTakenRaw/exposureTime/fNumberValue/focalLengthMm/focalLength35Mm + description (0x010E) — the display strings are locale-formatted and can't round-trip; old saved metadata without raw fields just embeds less. **Editable panel**: Metadata ▸ Authoring now has always-visible Description/Artist/Copyright fields (commit on blur/Enter, Esc reverts, quiet-until-focus styling). **Preferences ▸ Editing ▸ Attribution**: default author + copyright notice (`prefs.authorName/copyrightNotice`) used when a doc doesn't set its own. **Export As**: an "Embed metadata (EXIF/XMP)" toggle (single + batch; default on) with a summary line of what will embed; the **live size estimate includes the embedded bytes**; batch embeds per target at the target's pixel dims. **.gproj v9** persists doc metadata (older files open unchanged). Honest limits: AVIF not embedded; capture fields survive only for docs whose metadata was extracted after this change (raw twins), and a LUT-style "keep the ORIGINAL EXIF blob byte-for-byte" mode was deliberately not done — the model is parsed-and-rebuilt.
- [x] **Export presets + batch export + size estimate** — done 2026-07, all three sub-features in the reworked Export As dialog (`exportpresets.ts` + `zip.ts`, both dependency-free and Node-verified). **Estimated file size**: not a heuristic — the dialog actually ENCODES the composite with the current format/quality/scale/matte (debounced 280 ms so slider drags don't thrash the encoder) and shows the exact byte size ("~ 1.4 MB") in the preview header. **Presets**: four built-ins (PNG full, JPEG web 85%, WebP web 80%, PNG half-size) + user presets ("Save current" prompts a name; persisted under `graphiq:export-presets`, built-ins never persisted; deletable chips) — applying one sets format/quality/scale/transparent/matte in one click. **Batch export**: a Single/Batch segmented mode — batch rows are {format, scale %, quality, filename suffix}, defaulting to full-size PNG + half-size JPEG @0.5x; all targets encode from the SAME composite and download as **one .zip** (hand-written store-method writer in `zip.ts` — images are already compressed, so storing is correct and near-optimal; CRC-32, UTF-8 names with the language flag, central directory; ~120 lines). **Filename templates**: `{name} {w} {h} {scale} {ext} {n}` tokens expanded per target (unknown tokens survive verbatim), sanitized to safe basenames, colliding names auto-numbered (-2, -3, case-insensitive), with a live filename preview list in the dialog. **Verified**: 31 numeric checks (sanitize/template/target-name/dedupe logic, CRC-32 check vector CBF43926, and a structural parse-back of the written archive: signatures, EOCD counts, central-directory offsets, verbatim stored bytes) **plus a real-unzipper oracle** — PowerShell's Expand-Archive extracts a generated zip byte-perfectly incl. subdirectory + binary entries; tsc clean. Notes: batch targets keep alpha formats transparent (per-target mattes not modelled); single export still saves through the native file picker, batch always downloads the zip.
- [x] **LUT export (.cube)** — done 2026-07 (`app/lib/lut-export.ts` + `ExportLutDialog.tsx`; File ▸ Export LUT (.cube)… and an "Export as LUT" button in the Adjustments panel): captures the current colour grade as a standard **3D .cube LUT** by running an identity lattice (17³/33³/65³, red-fastest — the .cube row order) through the SAME pixel math the compositor uses. Two sources in the dialog: the **visible adjustment-layer stack** (bottom→top, honouring each layer's **opacity and blend mode** — Normal@100 runs pure math; modulated ops composite through a canvas, i.e. the browser's own blend implementation, identical to the engine's gco path) or the **Adjustments panel's current sliders**. Captures sliders (spatial sharpen/noise zeroed — kernels can't live in a colour LUT), Curves/Levels via their real LUT builders, and all the extra kinds. The dialog lists exactly what could NOT travel: Equalize (image-dependent), clipped layers, masks (ignored — a LUT is global), group-nested adjustments, spatial slider ops. Round-trip verified: an exported LUT re-imports through the app's own `parseCubeLUT`/Color Lookup and reproduces the direct adjustment (max error ≤ 2/255 from lattice quantization + trilinear; identity and invert exact in tests). 23 numeric checks; tsc clean. Note: capture runs the 8-bit sRGB pixel path — matching what's on screen for sRGB documents.

## 10. History

- [ ] **Snapshots** (pin a state; source for the History brush).
- [ ] **Non-linear option** (keep redo branch on new edits, PS-style toggle).
- [ ] **Undo limit setting** backed by a byte budget (large pixel patches accounted).
- [ ] Persist minimal history across reload (last N structural labels already saved in `.gproj`; consider replayable param-only steps).

## 11. UI / UX

- [x] **Command palette** — done 2026-07: a real top-centred palette on **`Ctrl+K`** (and clicking the top-bar search pill), replacing the old inline top-bar dropdown so there's ONE search, not two competing ones. **Fuzzy matcher** ([fuzzy.ts](app/lib/fuzzy.ts), dependency-free, 28 Node checks in scratchpad verify-fuzzy.ts): fzf-style — whitespace-separated tokens must ALL match as case-insensitive subsequences ("exp svg" → Export SVG); scoring favours word starts (+12), consecutive runs (+8), early anchors and short targets, with a multi-anchor search per token (greedy from every first-char occurrence) so "ca" in "a canvas" still lands on the word-start C; returns match indices for **highlighting** (`highlightRuns`). **Palette** ([CommandPalette.tsx](app/components/CommandPalette.tsx)): searches every tool (icon + key) and every executable menu item (menu-name subtitle + shortcut chip; a label miss retries against "label + menu" at half score so "window layers" works); panel toggles (Window ▸ …) show their live **on/off check** via the existing `checks` map; empty query shows a persisted **Recently used** list (`graphiq:palette-recents`, max 7); ↑/↓ wrap + scroll-into-view, Enter runs, Esc closes, click-outside closes. TopBar's pill became a **button** that opens the palette (inline dropdown code + styles removed — palette owns the UI); commands still build from the `menus.ts`/`tools.ts` registries, so new menu items appear automatically. Listed in the Keyboard Shortcuts window ("Application" section) + the Help docs. Deliberate limits: no accent folding in the matcher (documented in tests); recents are command keys, so renamed labels drop out gracefully.
- [x] **Keyboard Shortcuts window + registry + remapping** — done 2026-07 (the searchable window existed; this adds the registry and makes bindings REMAPPABLE). **Registry** ([shortcuts.ts](app/lib/shortcuts.ts), pure, 34 Node checks in scratchpad verify-shortcuts.ts): defs derive from tools.ts single letters + menus.ts shortcut strings + a couple of extra commands (Swap colours "X"), so display and dispatch share ONE source of truth; `parseShortcut` is a modifier-prefix scanner (handles the literal-plus key "Ctrl++"; "=" aliases to "+" so both layouts zoom), `eventToBinding` folds Meta into Ctrl, `canonicalBinding` powers the dispatch index and conflict detection; **overrides** (`graphiq:shortcuts`, string = rebound / null = unbound / absent = default) are validated on load and covered by the settings export/import/reset whitelist. **Keydown de-dup**: ~30 hardcoded menu-shortcut branches in Editor's global handler were REPLACED by one registry dispatch pass (`buildDispatchIndex` in a ref) — remapping really re-routes and unbinding really disables (the old hardcoded branches would have kept firing); copy/cut keep their no-selection fall-through so browser text-copy still works; kept hardcoded: contextual gestures (crop keys, brush [ ]/{ }/digits, Escape/Enter/Delete/Backspace, Shift+L/M cycles), the F-key action player, and two legacy aliases OUTSIDE the registry (plain Ctrl+N where browsers allow it, Ctrl+Y redo). **Bug fix for free**: menu shortcuts that were DISPLAYED but never bound — Ctrl+G Group, Ctrl+E Merge down, Ctrl+Shift+N New layer — now actually work. **Remap UI**: the Shortcuts window's sections now generate from the registry — click a key chip, press the new keys (pulsing "Press keys…" capture; Backspace unbinds; Esc cancels), conflicts are STOLEN from a remappable owner with a toast (reserved ones like Ctrl+V paste refuse — the paste EVENT owns it, it never enters the dispatch index), per-row restore-default + a confirm-guarded "Reset all (N changed)"; customized rows get an accent dot; curated canvas gestures stay read-only. **Live labels everywhere**: menus and the Ctrl+K palette render effective bindings through the registry (`shortcutLabels` → TopBar), so a remap shows up instantly; TOOL_BY_KEY retired (the registry preserves its first-match-wins rule for duplicate letters like Bucket/Gradient on G, plus the Shift+letter tool nicety). Honest limits: gestures aren't remappable by design; Ctrl+K itself is TopBar's listener (not in the registry); one binding per command (the legacy Ctrl+Y alias is hardcoded, not a second registry slot).
- [~] **Dockable/collapsible panel system v2** — three of four halves done 2026-07-18; **tabbed panel groups remain open**. Architecture: RightDock stays the ONE owner of the whole layout model and all panel rendering — the new **left dock** and **floating panels** render via `createPortal` into host divs Editor provides (`.leftDock` before CanvasArea, collapsing to nothing via `:empty`; `.floatHost` absolute overlay, pointer-events none/auto, kept BEFORE RightDock in the DOM so the dock stays the `:last-child` the UI-scale zoom selector targets — both hosts added to the zoom rule). Model: ONE global `order` (unchanged key) + `left: PanelId[]` membership + `floats: {id → {x,y}}` (new `graphiq:panel-layout` key, validated on load, in the settings whitelist). **Drag between docks**: the existing header-drag reorder now live-moves MEMBERSHIP when hovering a panel in the other dock, and each dock grows a dashed **drop zone** at its tail while a drag is live (the only way INTO an empty left dock). **Floating**: a **pin button** on every panel header floats it over the canvas (cascaded position) or docks it back; floats move by a grip bar (pointer drag, viewport deltas ÷ `uiZoom()` since the host is zoomed), raise on pointer-down, keep collapse working, cap at 72vh with internal scroll. **Workspaces** (Window ▸ Workspaces…): named snapshots of visibility + order + dock membership + floats + collapsed state under `graphiq:workspaces` (whitelisted) — save/apply/delete in a dialog; **Reset workspace now truly resets** (visibility AND the whole dock layout via the new `DockApi.apply(null)` — before, it only reset visibility). Still open: **tabbed panel groups** (merge panels into one tabbed frame — needs a group model layered over `order`) and drag-out-to-float (the pin button is the float affordance; HTML5 drag can't drop onto the canvas without a screen-wide drop layer fighting the dock drops).
- [ ] **Guides**: draggable from rulers, snap-to-guides, lock guides, smart guides (align hints between layers while moving).
- [ ] **Before/after compare** (split view or hold-\\ to peek at the pre-adjustment composite — the render graph can serve both cheaply).
- [ ] **Navigator**: zoom slider + click-to-zoom presets; **bird's-eye** (hold `H` + drag like PS).
- [ ] **Info panel** (live cursor readout: RGBA/HSB under pointer, selection W×H, doc coords).
- [x] **Properties panel** — done 2026-07 (`panels/PropertiesPanel.tsx`, in the right dock between Adjustments and Layers; Window ▸ Properties toggles it, drag-reorder/collapse persist like every panel — saved panel orders pick the new id up automatically). Fully contextual on the active node: kind badge + inline rename, blend-mode select, opacity slider, and a clip-to-below toggle for every node kind; pixel layers/groups additionally get **Masks** (layer-mask enable + link, filter-mask enable), **Effects** (per-effect enable toggles from the node's style, "Edit layer style…" into the dialog) and **Smart filters** (per-filter enable toggles, "Edit smart filters…"); adjustment layers show their type (registry label / Curves / Levels) with "Edit adjustment…" into their editor. Live-edit semantics match the Layers panel (un-labelled tree patches for slider drags; the toggle paths reuse the existing labelled ops — Enable/Disable Mask, layer-effect toggles, clip). Deep param editing deliberately links into the existing dialogs/panel rather than duplicating their live-session plumbing.
- [ ] **Touch & pen**: pinch zoom / two-finger pan on the canvas (pointer events are in place), pen pressure (see brush dynamics), palm rejection toggle.
- [~] **UI scale setting** — done 2026-07-18: **Preferences ▸ Appearance ▸ Interface scale** with four presets (Compact 90% / Default 100% / Comfortable 110% / Large 125%). Mechanism ([ui-scale.ts](app/lib/ui-scale.ts)): the choice persists in a `pe-uiscale` cookie rendered server-side as `data-uiscale` on `<html>` (same no-flash pattern as theme/accent; covered by settings export/import/reset via SETTINGS_COOKIES), which globals.scss maps to a `--ui-zoom` var; Editor.module.scss applies **CSS `zoom` to the CHROME only** — `.app > :not(.body)` (top bar, options bar, status bar, every dialog overlay) plus `.body`'s first/last children (toolbar, right dock) — while the middle child (CanvasArea: tabs, rulers, stage) stays at 100% so **document pixels are never resampled** (a "100%" zoom view stays 1:1). Body-portalled popups opt in via `zoom: var(--ui-zoom)` on their classes, and their placement math was fixed for the two-coordinate-space reality (gBCR/client coords are viewport px; offsetWidth/Height are local px ×z; style offsets on a zoomed element render ×z so writes divide by `uiZoom()`): Select dropdown, ColorPopover, GradientControl popover, the Layers context menu, and tooltips. `uiZoom()` returns 1 when the browser lacks standardized CSS zoom (`CSS.supports("zoom","2")`), so old browsers degrade to 100% with correct popups. Changes apply instantly (no reload). Still open from this bullet: **localization** scaffolding (strings are currently inline) — a large string-extraction pass, separate job.
- [ ] Colourblind-safe accent option + a **high-contrast** theme variant; full keyboard-navigation audit (focus rings exist — ensure every panel/dialog is traversable).
- [x] **Onboarding** — done 2026-07-19: an **interactive spotlight tour** + a **sample document**. **Tour** ([tour.ts](app/lib/tour.ts) steps + [TourOverlay](app/components/TourOverlay.tsx)): 8 steps — centred welcome/finish cards plus six spotlit chrome regions found by new **`data-tour` attributes** on TopBar/OptionsBar/Toolbar/CanvasArea/RightDock/StatusBar (measured live per step + on resize, so rearranged docks still target correctly). The spotlight is ONE element — a rounded hole whose `200vmax` box-shadow dims everything else (animated between steps, `data-motion="off"` respected); the card places below-else-above the target, clamped on screen; ←/→/Enter step, Esc leaves; the overlay owns the keyboard while open (capture-phase `stopImmediatePropagation` so tool shortcuts can't fire). Overlay lives in the zoomed chrome subtree → all viewport coords **divide by `uiZoom()`** (the established popup rule). **First run**: no `graphiq:welcomed` flag → the tour greets automatically; Skip/Done both set the flag; the key is in the settings whitelist, so **Restore defaults re-onboards** and exports carry it. Re-run any time via **Help ▸ Interactive tour**. **Sample document** (**Help ▸ Open sample document**, also offered on the tour's last step): fully procedural — no assets — three canvases (sky gradient + sun glow, translucent blobs + a brushy bezier stroke, a text headline) loaded through the standard PendingLoad machinery into a real tree: Headline (yellow label), an **expanded group** holding Floating shapes (green label, 92% opacity), Sky — so layers, groups, labels and opacity are all immediately explorable. Both actions are in the palette automatically and excluded from macro recording.

## 12. Settings (Preferences additions)

- [x] **Performance tab** — complete 2026-07: render-cache **budget slider + live stats** (done earlier: 64–1024 MB, usage meter, product count, session hit rate, debug on/off), plus **Undo steps** (10–200, default 60 — `prefs.historyLimit` → engine `setHistoryLimit`, trimming the OLDEST entries after every push and on shrink; pixel patches are the dominant memory holders) and a **Background workers** toggle (`prefs.useWorkers` → engine `setWorkersEnabled` gating the blur/filters/heal `ensure*Worker` fns + `setRawWorkerEnabled` for the DNG decoder — every path already had a synchronous fallback, so off = the debugging-friendly inline mode; in-flight jobs still land).
- [x] **Storage tab** — done 2026-07 with the Settings/Help-stubs item (§1): origin storage estimate + meter, persistent-storage status/request, and per-store breakdown (autosave snapshot / recents / presets & swatches) with confirm-guarded clear actions; opened directly by Settings ▸ Scratch disks / storage… (helpers in `app/lib/storage.ts`).
- [x] **Cursors tab** — done 2026-07-19: Preferences gained a **Cursors** tab (Crosshair icon). New prefs (`paintCursor: "ring" | "precise"` default ring, `brushCrosshair` default true, `ringColor` default #ffffff — defaults reproduce the old cursor exactly; whole-prefs-key settings coverage automatic). CanvasArea's overlay cursor code (the deps-[] draw callback → values flow through a new `cursorPrefsRef`) was refactored around one **`drawBrushCursor`** helper used by ALL seven ring sites (brush/pencil/eraser, heal, red-eye, blur, dodge/burn, clone): **precise mode** draws a larger centre crosshair only (no size preview), ring mode draws the ring + a now-OPTIONAL centre crosshair. **Ring colour**: the light strokes of the ring, dashed hardness ring, crosshair, and the clone tool's source marker/connector/Alt-reticle all take the pref colour (hex→rgba at each stroke's alpha) — the dark under-strokes stay, so any colour keeps reading on light pixels. A `[cursorPrefs]` effect kicks the overlay repaint so changes show mid-hover without waiting for a pointer move. The tab: a live **SVG preview** over a light→dark gradient (judges the two-stroke contrast trick), ring/precise OptionList, the crosshair toggle (shown only in ring mode), and the colour well. OS-cursor behaviour unchanged (hidden over the canvas for these tools — the overlay IS the cursor).
- [x] **Units & rulers** — done 2026-07: Preferences gained a **Units & rulers** tab — measurement unit (**px / in / cm**, `prefs.unit`) + **default resolution** slider (72–600 ppi, `prefs.defaultDpi`). Documents now carry a **`dpi`** (stamped from the preference at creation, editable per-document in the New document dialog's new Resolution field, serialized in `.gproj` as an optional field — old files open at 300). Unit-aware surfaces: **rulers** tick/label in the chosen unit (nice-step spacing computed in unit space, decimals trimmed; pixels keep integer labels), the **status bar** shows physical dimensions with the ppi (`6.40 × 3.60 in @ 300 ppi`), the **Metadata panel** gained a Resolution row with the physical size, and **printing is true-size** (the print CSS sets the image width to `width/ppi` inches, still fitting the page when larger). Deliberately still pixel-based: the Image Size / Canvas Size / New-document size FIELDS (unit-aware size entry is a follow-up).
- [x] **Transparency grid** — done 2026-07: Preferences gained a **Transparency** tab (Grid2x2 icon) controlling the canvas checkerboard. **Grid size**: None (flat backdrop, no squares) / Small 8 px / Medium 16 px (default) / Large 24 px — screen-space like before, so squares keep their on-screen size at every zoom. **Grid colors**: Match theme (default — the existing `--checker-a/b` vars, so it keeps tracking light/dark), the classic Photoshop **Light** (#fff/#ccc) / **Medium** (#ccc/#999) / **Dark** (#999/#666) pairs, or **Custom** with two colour wells (native colour inputs styled as Magiq swatch chips, hex shown beside). A live **preview strip** in the tab shows the exact result. Plumbing: `prefs.checkerSize/checkerColors/checkerA/checkerB` (defaults reproduce the old look exactly; old prefs files pick up defaults; garbage values fall back safely) + one shared resolver `checkerCSS()` in prefs.ts used by BOTH the canvas checker div (CanvasArea, inline style over the `.checker` class) and the dialog preview, so they can never drift. Settings export/import/reset cover it automatically (whole-prefs-key whitelist). Deliberately unchanged: the small UI-chrome checkers (colour-picker alpha strip, navigator artwork, paste/export dialog thumbs, gradient bar) stay theme-based — the preference is about the document's transparency rendering, not the chrome.
- [x] **Guides & grid** — done 2026-07-19: Preferences gained a **Guides & grid** tab (Grid3x3 icon) and the canvas a real **document grid**. New prefs (`gridSpacing` 8–256 px default 64, `gridSubdivisions` 1–8 default 4, `gridColor` default #4688ec, `pixelGridColor` default #808080 — reproduces the historical grey exactly, `snapDistance` 2–16 px default 6 — reproduces the historical SNAP_PX): all ride the whole-prefs-key settings export/import/reset automatically. **Document grid**: a NEW `View ▸ Document grid` toggle (persisted in `pe-view` as `docgrid`, checkable in the menu/palette) draws gridlines every `spacing` doc px with fainter subdivision lines, at ANY zoom, in the same overlay canvas as the pixel grid (doc grid beneath, pixel grid on top) — lines auto-hide when they'd be denser than ~4 screen px, so tiny zooms never become a solid wash. **Colours**: both grids take their pref colour (hex → rgba at the grids' alphas). **Snap distance**: the shape tools' node-symmetry snaps (`SNAP_PX` was hardcoded 6) now use `prefs.snapDistance`; the tab honestly explains that View ▸ Snap's whole-pixel selection snapping is an on/off, not a distance. The pixel-grid behaviour (Ctrl+', shows from 400% zoom) is unchanged apart from the colour. NOTE: **draggable guides** (from rulers, snap-to-guide, smart guides) remain the separate §11 item — this tab is the settings/grid half, and `snapDistance` is sized to serve guide snapping when §11 lands.
- [x] **Files tab** — done 2026-07: Preferences gained a **Files** tab (FolderOpen icon) gathering the file-handling settings. **Autosave interval** MOVED here from Editing (same slider/pref, 0–10 min; RestoreDialog + Help docs references updated; the New-document toggle stays in Editing). **Export defaults** (new prefs `defaultExportFormatId`/`defaultExportQuality`): an option list of the formats this browser can actually encode (`availableFormats()`, so AVIF/WebP only appear when real) + a default-quality slider — Export As opens preselected with them (`initialFormatId`/`initialQuality` props with fall-back-to-PNG for unencodable saved ids; everything stays editable in the dialog). **Recent files length** (new pref `recentsLimit`, 4–20, default 8): recents.ts's hardcoded MAX_RECENTS became a module setter (`setRecentsLimit`, clamped 1–20) driven by an Editor pref effect; honest semantics — the list trims as entries are ADDED, so shrinking applies on the next save, and the Storage tab remains the way to clear outright. **Libraries**: the "Share saved gradients" toggle MOVED here from Editing ▸ Gradients (the snap-midpoint toggle stays), as the seed of the future shared-libraries section. All new prefs ride the existing whole-prefs-key settings export/import/reset automatically.
- [x] **Reset all preferences + settings import/export (JSON)** — done 2026-07 (`app/lib/settings.ts` + the Preferences footer). **Restore defaults…** (confirm → clear + reload) resets everything configuration: the Preferences model, tool options, panel order/visibility, view toggles, working colour space, proof target, and the theme/accent cookies — while deliberately KEEPING user content (saved gradients, swatches, adjustment presets, recents, autosave snapshots). **Export settings** downloads `graphiq-settings.json` (`{format:"graphiq-settings", version:1, values, cookies}`, localStorage values stored opaquely); **Import settings…** validates the format and writes ONLY whitelisted keys (a crafted file can't touch user content or foreign keys — verified), then reloads — a reload is the one honest way to apply everything atomically, since consumers read their keys on boot. Absent keys in an import mean "that setting's default" (removed, not skipped), so an export taken on defaults restores defaults.

## 13. Panels (new)

- [~] **Info** (see §11) still open; ~~**Properties**~~ (done, see §11) and ~~**Paths**~~ (done 2026-07, see §6) are shipped.
- [x] **Swatches v2** — done 2026-07-18: a dedicated **Swatches panel** (SwatchBook icon, Window ▸ Swatches, after Color; saved orders/workspaces pick the id up automatically) over a reworked [swatches.ts](app/lib/swatches.ts) (27-check verify-swatches.ts). **Groups**: the store became `{version:2, groups:[{id,name,colors}]}` under the SAME `graphiq:swatches` key — a legacy flat list migrates into one "My swatches" group, and every ColorPicker keeps working untouched through flat compat shims that read/write the FIRST group (its strip = "your quick swatches"). Panel UX: collapsible groups with double-click rename, add-current-colour, delete; click a swatch → foreground, Alt/right-click removes; selected-colour ring. **Import/export `.gco`/`.gse`/JSON**: hand-written **binary codecs of the Adobe ACO and ASE formats** under the app's g-extensions (plain `.aco`/`.ase` accepted too — same bytes): ACO writes v1+v2 blocks (big-endian, RGB ×257, UTF-16BE names incl. the length-with-terminator quirk) and flattens groups; **ASE keeps groups as real folders** (`ASEF`, 0xC001/0xC002/0x0001 blocks, f32 RGB, non-ASCII names verified) — so `.gse` is the round-trip format; JSON gained a v2 groups shape (v1/bare/GPL/loose-hex still parse); ONE unified `parseSwatchImport(buffer)` sniffs ASE by signature, then ACO, then text. **Palette extraction** ("From image"): **median cut** over the ≤64px-downsampled composite → an 8-colour group named "{doc} palette" — with two real algorithm fixes the tests forced: box selection scores **range × population** (pure widest-range degenerates when several boxes tie at full range), and splits snap to the **value boundary nearest the median** (raw median-index splits land inside tie runs when few distinct colours dominate, wasting the budget halving identical pixels — a 5% white population never separated until this). Transparent pixels ignored; uniform image → one colour, no infinite split. Registered across RightDock/Editor/menus per the panel recipe; `graphiq:swatches` already sat in the user-content PRESET_KEYS.
- [ ] **Brushes panel** (preset management with previews, import ABR-subset?).
- [x] **Actions panel** — done 2026-07 (the §14 recorder's core): a new right-dock **Actions** panel (Zap icon, Window ▸ Actions, default hidden; saved panel orders pick it up automatically) that **records document menu commands into named, replayable macros**. Model in [actions.ts](app/lib/actions.ts) (73-check verify-actions.ts): `SavedAction {id, name, fkey, steps[{action, label}]}` persisted under `graphiq:actions` (user content → covered by the Storage tab's presets row + clear, NOT the settings whitelist); an explicit **recordability allowlist** — only commands that replay unattended (undo/redo, cut/copy/paste, content-aware fill, save/print, crop-to-selection, rotates/flips, all layer/mask/clip ops, select all/deselect/reselect/inverse, the preset adjustment layers, invert/equalize, fx-add-*/fx-clear) — with dialog-openers, parameterized adjustment editors, smart-filter adds (they open their stack dialog), free transform and view/window/UI commands excluded; the verify script asserts every allowlisted id EXISTS in menus.ts (rename guard) and that the excluded classes stay excluded. **Recording** hooks the ONE dispatch chokepoint (`handleMenuAction` — menus, palette and shortcuts all funnel through it): a red pulsing banner shows the armed action + live step count, click to stop. **Playback** re-dispatches ids through a ref to the LATEST handler with a 60 ms gap per step (React must commit state + re-point the handler refs between steps), guarded against playing-while-recording and self-appending. **F-keys**: assign F2–F10 per action (F1/F5/F11/F12 stay with the browser; assigning steals the key from any other action), dispatched from the global keydown listener. Panel UX: name input + Record, rows with rename (double-click), F-key select, Play/Delete, expandable numbered step list with per-step remove. UPDATE 2026-07: brush/pencil/eraser strokes are now ALSO recorded (see §14 for the StrokeStep layer); dialog settings, other tools' gestures and view changes remain uncaptured — §14's batch processing + scripting hook stay open.
- [x] **Histogram panel upgrades** — done 2026-07-18, all three in the Channels panel. **Luminosity mode**: `ChannelHistogram` gained an `l` array (Rec.709 weights, computed in the engine's same pixel scan) and the panel a fourth "Lum" toggle (near-white dot, default off) — its curve draws **source-over BENEATH the additive RGB curves** (adding white into the `lighter` blend would blow the overlaps out) and shares the fixed all-channel normalization so toggling stays stable. **Clipping warnings**: a row under the graph shows the share of pixels clipped to pure black / pure white — the WORST fraction across the enabled channels (luminosity when none are on; one total serves all channels since each counts the same opaque population) — tinted `--danger` above 0.5%, with explanatory tooltips. **Per-selection histogram**: `histogram(tree, sel?, angle?, pivot?)` rasterizes the selection via the existing `selectionMask` and **downsamples the mask with the composite** (same ≤480px cap — the scan stays cheap on any doc size; alpha ≥128 = inside), so with a live selection the whole panel (curves, clipping, mask row unaffected) reflects only the selected pixels, flagged by an accent "Selection" badge on the graph; recomputes ride the existing debounced engine subscription plus the selection props (threaded Editor → RightDock → panel). Rotated selections honoured (mask rotates about the same pivot as the content).

## 14. Automation

- [x] **Actions / macro recorder** — DONE 2026-07 in two layers. (1) Menu-command core (shipped as the §13 Actions panel): record replay-safe `handleMenuAction` commands, play back, F2–F10 assignment. (2) **Recordable paint strokes** — while recording, every finished **brush / pencil / eraser** stroke is captured as a `StrokeStep` (`actions.ts`): the tool, a snapshot of its `BrushSettings` (size/hardness/opacity/flow/blend/smoothing), the paint colour, and the raw pointer path in document space (thinned to ≥0.75 px spacing). Capture lives at CanvasArea's begin/move/end stroke sites behind a `recordStrokes` prop (never during playback), and playback replays the identical points through the live `engine.beginStroke/moveStroke/endStroke` pipeline (`playStroke` EngineHandle verb), so smoothing, spacing, blending and undo journaling behave exactly as a live stroke — targeting the CURRENT active layer and CURRENT selection ("as if you did it now" semantics, same as command steps). Steps list shows "Brush stroke (n pts)" rows, individually deletable; stored strokes are validated/coerced on load (bad points filtered, partial brush settings merged over defaults; Node suite: 82 checks). Honest remaining limits: clone/heal/dodge dynamics, gradient/shape/text gestures and dialog settings are still not captured — that niche (parameterizing arbitrary gestures) is out of scope for this bullet.
- [x] **Batch processing** — done 2026-07-18: **File ▸ Batch process…** ([BatchDialog](app/components/BatchDialog.tsx)) runs a saved **Action** and/or an **export preset** over picked-or-dropped files. Per file, Editor's `batchRunFile` opens it as a **temporary document through the real app**: decode via `decodeImageFile` (so TIFF/DNG/HEIF etc. all work), doc + PendingLoad machinery, **poll until CanvasArea consumes the load** (the engine holds the pixels; 8 s timeout → error row) plus a settle tick, then `playActionRef` replays the action exactly like the F-key path (menu commands AND recorded strokes — refs re-point between steps as usual), `exportComposite` grabs the flattened result, and a `finally` closes the temp doc functionally (the ambient `closeDoc` reads render-scope state and would go stale inside the async loop) and restores the previously active tab. The DIALOG owns the loop: strictly sequential, live "n of m: name" progress, **Stop after this file**, per-file error collection (decode/encode failures never abort the batch), preset encoding via `renderExport` (builtin + user presets from exportpresets.ts; format falls back when the browser can't encode it), names = sanitized stems + `dedupeFilenames`, download as **one store-method zip** (`batch-YYYY-MM-DD.zip`) or directly when a single file survived. `.gproj` files are filtered from the drop (they're projects, not images). Honest notes (also in the dialog): the canvas visibly flicks through files while running — the batch IS the live app, which is what makes action replay faithful; actions that pop dialogs can't be recorded in the first place, so batches never stall.
- [x] **Scripting hook** — done 2026-07-19 (§14 is now COMPLETE): **`window.graphiq`** ([scripting.ts](app/lib/scripting.ts), 18-check verify-scripting.ts on the pure parts). **Safe by construction**: nothing exposes the raw engine — every mutation routes through the SAME paths the UI uses (menu-command dispatch = journaled/undoable exactly like clicking; the Layers panel's `LayersApi`; the Actions player; the export compositor), so a console snippet can't reach a state the UI couldn't. Surface (v1, printed by **`graphiq.help()`**): `docs()/activate(id)` tabs; `commands()/run(id)` (every executable menu id, validated with a console warning on unknowns); `layers()` (plain serializable tree summary — internal fields verified not to leak), `selectLayer(id)`, `setLayer(id, {name/visible/opacity/blend})` (values clamped/validated); `foreground()/setForeground(hex)`; `select(rects|null)` (finite-checked); `undo()/redo()`; `actions()` + `await play(idOrName)` (id-first resolution, then first name match); `await exportPNG(scale?)` → Blob and `await download(name?, scale?)`. Install: built ONCE in an Editor effect, reading a **deps ref re-pointed every render** (fresh render-scope functions; the engine handle passes as the ref itself since CanvasArea re-points it per doc); cleans up on unmount; announces itself with a console.info. Documented in `graphiq.help()`, the Help dialog and FEATURES. Honest framing: a dev tool — `version` bumps if the shape changes; no pixel-level engine verbs exposed (that's what actions/batch are for).

## 15. Quality & infrastructure

- [ ] **Unit tests for the pure libs** (`tone.ts`, `filters.ts`, `gradient.ts`, `render-graph.ts`, `layers.ts` tree ops, `view.ts` invertRects) — they're dependency-free and crying out for Vitest.
- [ ] **Pixel-identity harness**: automated A/B of cache-on vs `disableRenderCache` composites over fixture documents (the Spec 06 oracle, CI-ified).
- [ ] **Golden-image tests** for filters/effects (hash composite outputs of fixture docs).
- [ ] **Perf benchmarks** (composite time vs layer count; filter throughput) tracked over time.
- [ ] **E2E smoke** (Playwright): boot, paint, undo, save/open, export.
- [ ] Error boundary + user-facing crash report dialog with "save recovery copy".

---

### Suggested next three (highest value ÷ effort)

Round 1 — all done 2026-07: ~~§1 polish sweep~~ · ~~healing brush + content-aware fill~~ · ~~worker offload (Blur Gallery)~~.

1. ~~**Autosave & crash recovery**~~ — done 2026-07: IndexedDB snapshots on a Preferences interval (default 2 min, only when history moved), a `pagehide` heartbeat that detects unclean exits, a Restore/Discard prompt on boot after a crash, and a real save-state indicator in the status bar. **Now snapshots ALL open documents** (see §8), not just the active tab.
2. ~~**Smart-filter stack mask**~~ (§1) — done 2026-07: one grayscale filter mask per layer/group confining the whole stack (white = filtered, black = original, premultiplied blend between). Lives in the engine's existing mask maps under `fm:<id>`, so painting (brush/pencil/eraser/fill/gradient/blur/dodge), history's mask surface, undo/redo and restore all reuse the layer-mask machinery. Managed from the Smart Filters dialog (Add / From Selection / Enable / Paint / Delete) + a dashed flask chip in the Layers panel (click = paint target, Shift-click = enable/disable). Serialized as `filterMaskImage` in `.gproj` v8 (older files open unchanged); duplicated, transformed (resize/rotate/flip/crop) and consumed by "Apply (Bake)" correctly. Not included: unlinked filter-mask move (it always tracks the layer).
3. **Layer lock flags** (§4) — the search/filter half of this bundle is DONE 2026-07 (colour labels + filter row, see §4), and the command-palette runner-up shipped too (§11). Remaining: transparency/pixels/position/all locks enforced in the engine's paint guards + header checkboxes.
